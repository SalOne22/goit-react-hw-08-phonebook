import { LoginForm } from './LoginForm';
import { Section } from '~/components/ui/Section';

const Login = () => {
  return (
    <Section title="Login">
      <LoginForm />
    </Section>
  );
};
export default Login;
